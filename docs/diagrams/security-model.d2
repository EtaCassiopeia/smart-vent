trust: TRUST BOUNDARY {
  style.stroke: "#e74c3c"
  style.stroke-width: 3

  thread: Thread Network {
    style.stroke: "#2ecc71"

    encryption: AES-128 encrypted\nat MAC layer
    membership: Network membership\n= authorization
    joining: Joining requires\nnetwork key
  }

  coap: "CoAP: No additional auth\nOn the Thread network = trusted\nThe network key IS the credential" {
    style.stroke: "#e68a00"
  }

  otbr_api: "OTBR REST API\nLocalhost only (127.0.0.1:8081)" {
    style.stroke: "#3a7bd5"
  }

  ha: "Home Assistant\nLocal network, has its own auth" {
    style.stroke: "#3a7bd5"
  }
}

no_internet: "NO INTERNET ACCESS for Thread mesh\nNO CLOUD SERVICES\nNO REMOTE ACCESS" {
  shape: text
  style.font-size: 14
  style.font-color: "#e74c3c"
  style.bold: true
}
